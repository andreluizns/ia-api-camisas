// src/app/(auth)/login/page.tsx
import { LoginForm } from "@/components/auth/login-form"

export const metadata = { title: "Login — Admin Camisas" }

export default function LoginPage() {
  return <LoginForm />
}
